#include "io.h"

void StringExtract()
{
	// 테스트 케이스의 개수
	char Word[10];
	int WordSize = sizeof(Word) - 1;

	cout << "문자열을 입력해주세요 : ";
	cin >> Word;

	// 문자열의 맨 앞 & 맨 뒤 글자 추출 
	// abidleg -> ag

	cout << Word[0] << " / " << Word[];

	/*
	int InputWord; 

	// 문자열 입력
	cout << "문자열을 입력하시오 : ";
	cin >> InputWord;

	for (int t = 0; t < InputWord; t++)
	{
		string input;
		cin >> input;

		cout << input[0] << " / " << input[input.length() - 1];
	}
	*/
}